name: Dumka
description: 'Dumka Project'

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


\
  # UI
  google_fonts: ^1.1.0
  custom_switch_button: ^0.5.0
  material_design_icons_flutter: ^4.0.5345
  flutter_svg: ^0.19.1


dev_dependencies:
  flutter_test:
    sdk: flutter


  lint: ^1.2.0
#  pedantic: ^1.9.2

flutter:
  uses-material-design: false

  assets:
    - assets/
    - google_fonts/