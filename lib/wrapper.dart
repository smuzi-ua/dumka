import 'package:flutter/material.dart';

import 'screens/user/user_wrapper.dart';

class DumkaAppWrapper extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // todo listen to bloc

    return UserWrapper();
  }
}
